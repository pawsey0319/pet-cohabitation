# 原型检查记录

检查日期：2026-09-10。范围为独立 HTML 设计原型，不代表安卓 App 或真实后台验收通过。

## 生成与整理

OpenDesign 0.22.1 中使用用户选择的 Grok Build `grok-4.6` 生成 `index.html`、`DESIGN.md` 和 `HANDOFF.md`。运行 ID：`71b99e22-d1de-4b4d-bfbc-6926fedd9078`，OpenDesign 返回生成成功，入口为 `index.html`。

Codex 核对并修正了手机画布溢出、异宠形象与气泡共用类名造成的尺寸冲突、按钮文字对比度、接续原话定位、错误记忆的展示和示例回应、图片预览与已应用背景的隔离、搜索焦点、编辑草稿后重试，以及首次聊天的首条消息显示。原型采用同一 HTML 文件，未修改现有 App 源码或后台。

## 已检查

- 最终文件经 OpenDesign 的预览接口重新加载，无页面 JavaScript 错误；与本地文件内容一致。
- 360 / 390 / 430px 下陪伴、记忆及背景页面无横向溢出。前一轮另检查了消息、群聊、邀请、Agent、任务、成长、表单、登录注册等主要页面的大字号深色布局，共 17 个页面类别、51 个宽度组合。
- 短对话输入区紧随上下文；长对话输入区仍可见。发送后输入清空，失败恢复草稿，修改后重试只出现一次用户消息；首次聊天能显示新消息。
- 合成记忆支持纠正、忘记、来源查看及继续聊天；演示回复不继续引用已纠正或忘记的示例偏好。接续来源与卡片原话一致。
- 图片通过浏览器本地文件选择预览。全局背景与单聊覆盖独立，恢复全局生效；改变尚未应用的图片不改变已应用背景。横向图片的裁切覆盖经过检查。
- AI 背景显示“演示效果”，支持候选与应用。没有调用真实图片模型。
- 消息搜索连续输入、群聊创建与邀请演示、任务入口、异宠期待到生成确认、手工记忆保存、注册密码不一致校验可操作。
- 主按钮文字对比度：浅色约 **5.31:1**，深色约 **5.46:1**。这不是任意上传图片、所有文字及控件的完整无障碍审计。

最后同步后的专项复测结果、浏览器版本、SHA-256 与页面错误列表保存在 `verification.json`。预览图位于 `previews/`。

## 演示边界与后续验证

- 所有对话、记忆、人物和任务均为合成数据，自动提取、真实模型、跨客户端并发及云端数据隔离未在本原型实现。
- 设置、草稿和图片保存在当前页面内存，刷新会重置。生产版必须继续使用现有 App 的持久化、请求标识和记忆版本校验。
- 退出、注销、建群和邀请只演示状态，不执行真实账号或对外操作。原型文本导出不是用户完整数据备份；OpenDesign 预览沙箱可能限制下载，可直接打开本地 HTML 使用浏览器下载。
- 当前异宠 SVG 是设计占位，正式接入沿用现有宠物形象。
- 安卓输入法、返回键、TalkBack、系统字号、真实相册权限和大图内存仍需接入 App 后真机验证。浏览器改变视口不能替代安卓键盘验收。
- 背景的真实上传/存储和 AI 生成属于后续功能接入，本次只交付设计。
