请使用当前已选择的 Grok Build / grok-4.6 完成《异宠》安卓 App 的整套高保真前端重设计。需求访谈和方向已经确认，直接完成可运行设计，不再做方向小样，不再追问或停在计划。使用 OpenDesign 的 frontend-design 能力。

交付范围：仅此独立 OpenDesign 项目中的设计原型；不要访问或修改现有 App 仓库，不接真实后台，不部署，不登录其他服务，不使用付费图片生成。所有对话/账号/记忆均为合成演示数据。提交可以直接预览的 index.html（可配套本地 CSS/JS）、DESIGN.md 设计规范、HANDOFF.md React Native 迁移与状态说明。优先无外部依赖、无构建步骤的 HTML/CSS/JS。所有页面中文，所有可见按钮有合理动作，主流程真正可点击。

已确认风格：成年用户的日常陪伴产品；参考微信清爽、整齐、商务一点的气质，但不用微信品牌、Logo或照搬界面。白与极浅灰为主要面积，少量暖杏/陶土色点缀。克制、平面、细线、明确对齐、舒适留白、精致排版。避免幼稚游戏感、巨大圆角卡片堆叠、渐变大标题、大装饰头像和营销落地页。中文系统无衬线字体，正文约16–17px，行高舒适。制作完整浅色和深色主题。保持手机应用感；桌面宽屏可将原型居中，外围辅助操作不混入手机产品界面。

信息架构不能重做：底部三个入口“消息 / 异宠 / 我的”，默认打开“异宠”的“陪伴”。异宠还有“消息管家 / 成长”独立入口。陪伴首页有克制的异宠形象、近期接续、真实高度的对话和输入区，顶部可进入记忆管理。消息页右上角不要通知铃铛。保留已有主 Agent 工作台、群聊、建群/邀请、个人资料/设置、登录/注册、隐私/导出/注销的相应入口；未确认异宠为“填写期待 → 生成 → 确认”，不要五轮孵化。

关键页面与交互（请完整覆盖，不只做首页）：
1. 陪伴：温和有辨识度的小异宠形象，可用自绘SVG作为清楚的设计占位，交接说明正式版本沿用用户现有宠物形象。轻微呼吸/倾听/思考状态；长聊、输入时自动缩小；支持减少动态。两侧气泡，用户气泡轻度暖色，异宠更浅。示例以真实朴素的谈话展示倾听与变化，不虚构用户经历。
2. 聊天输入区是特别要求：放在当前对话上下文的下方。对话很短时紧接内容，不能硬贴到整屏最底部造成巨大空白。长对话可滚动，聚焦输入后输入与发送都可见；用 visualViewport 等合理处理移动键盘，避免正文和键盘遮挡。提交立即清空输入，显示诚实的回应等待；失败可恢复草稿/重试。包含正常、正在回应、失败/重试、长消息的演示状态。只有用户点“新话题”才改变话题边界。
3. 近期接续：日期、合成原话引用和继续/收起/新话题。明确可查看依据，不编造重逢事件；继续后进入当前对话。
4. 记忆管理：区分当前理解、过去偏好和变化依据，有原话及日期；查看来源、手工新增/编辑（20条、每条400字）、标记重要、纠正、忘记、提取失败后重试。用咖啡→最近偏爱茶展示变化，不自动认定讨厌咖啡；“不再喜欢”“我从没喜欢过”展示不同状态。纠正/忘记后仍可继续当前话题。不要向用户展示漂移小数分数。后台逻辑此原型仅模拟，文档说明真实接入点。
5. 消息列表：干净行布局、未读/时间/摘要、搜索；可进入群聊，群聊同样有正常输入区、发送与失败状态，邀请流程和任务交给 Agent 的入口。
6. 消息管家与主 Agent 工作台：功能性查询/摘要/任务跟进，使用和陪伴一致的设计语言，但个人记忆不出现在这里。列出合成任务，详情/状态/重试等动作可用。
7. 成长：既有成长记录、形象和操作，清楚、克制，不增恋爱等级、签到或共同养宠；未确认异宠的期待输入→生成中→可重试→预览→确认流程可从原型演示工具进入，不破坏默认已确认宠物。
8. 我的/设置：资料、外观（跟随系统/浅/深）、减少动态、隐私、数据导出、退出/注销确认等有合理页面/对话框，演示不真实删除数据。登录/注册表单展示校验及状态。
9. 重点新增“聊天背景”设计（本轮仅交互演示）：默认/推荐背景、从图片选择、AI设计三条入口。只改变聊天背景及协调的气泡/文字颜色；只影响自己看到的界面。支持全局所有聊天，单个对话覆盖及恢复全局。默认/推荐不是按时间或宠物状态不停自动生成。
   图片流程：用户通过真实文件选择在浏览器本地预览→裁切/位置与暗度/可读性调整→在带真实聊天气泡的预览中确认→应用。不要上传文件。
   AI流程：输入希望的画面→演示生成中→若干候选预览→修改描述/重新生成→应用；用内置CSS/SVG样例模拟并明确标注“演示效果”，不能声称已真实调用图片模型。建议纹理/淡景背景，文字可读性始终优先。
   更换背景时在聊天预览同时配套气泡和文字颜色，保留文字对比度。设置清楚说明“仅自己可见”。

质量要求：390px为主要画布，适配360/430px，不水平溢出；暗色同样精致，不简单反色。触控目标约48px，正文对比度目标4.5:1，支持键盘导航、表单标签、可见焦点、prefers-reduced-motion。图标使用统一轻细SVG体系，避免emoji代替界面图标。允许合理克制的SVG宠物/背景插画，不依赖网络图片。演示工具（例如切换错误/加载/首次进入/字号）放在桌面原型工具栏或独立说明区域，与正式产品区分。

请先形成内部一致的设计系统，再直接编写并检查完整原型文件。保证页面跳转、记忆操作、短长对话输入、背景的全局/单聊覆盖、浅深主题、生成/确认均可点击。DESIGN.md 记录令牌和设计决策；HANDOFF.md 列页面与组件/状态、哪些是演示、现有 RN 功能对接处和不新增原生依赖的策略。完成时给出文件列表与尚需真机验证的限制。